{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/c_fft_visualizer",
            "args": [
                "/home/avic/Music/03. GLBTM (Studio Outtakes).ogg",
                "--loop"
            ],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build",
            "initCommands": [
                "settings set target.x86-disassembly-flavor intel"
            ]
        }
    ]
}
